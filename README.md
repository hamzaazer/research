# research
A zero-dependency, hyper-optimized, cross-platform memory scanner + pattern sniffer written in C. It doesn't ask for permissions. It doesn't log. It doesn't care about your OS. It finds what it wants and leaves no trace.
